import Modal from "../Modal/Modal";
import UploadPhoto from "../../UploadPhoto/UploadPhoto";
import ModalProps from "../ModalTypes";

const PhotoModal = ({ modal, setModal }: ModalProps) => {
  return (
    <div>
      
    </div>
  );
};

export default PhotoModal;
